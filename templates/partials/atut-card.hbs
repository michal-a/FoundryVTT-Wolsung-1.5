<div class="atut-card">
    <div class="atut-name" data-field="name">
        <a class="item-edit removeble" data-itemid="{{id}}" title="{{localize 'wolsung.contextMenu.edit'}}">{{name}}</a>
    </div>
    <div class="atut-opis">
        <p>{{cleartext data.data.opis}}</p>
    </div>
</div>